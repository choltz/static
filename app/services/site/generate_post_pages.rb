Namespace.build('Services::Site::GeneratePostPages') do
  include Services::Base

  # Public: Given a list of posts, process them into markup files
  #
  # options - parameter hash
  #   posts: array of posts
  #
  # Returns: unmodified parameter hash
  def call(options = {})
    options.tap do |opts|
    end
  end
end
