Namespace.build('Services::Site::WriteFiles') do
  include Services::Base

  # Public: Given a list of site file data, write the results to files
  #
  # options - parameter hash
  #   posts: array of post data
  #
  # Returns: unmodified parameter hash
  def call(options = {})
    options.tap do |opts|
    end
  end
end
